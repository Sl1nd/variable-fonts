before_script:
  - npm install
pages:
  stage: deploy
  script:
    - npm run cp
    - mkdir .public
    - cp -r * .public
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - master
